[project]
name = "sfc-drl"
version = "0.1.0"
description = "DRL for SFC Provisioning"
requires-python = ">=3.9"

dependencies = [
    "torch>=2.0.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "hydra-core>=1.3.0",
    "omegaconf>=2.3.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.3.0",
]